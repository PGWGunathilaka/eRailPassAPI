import { TClass } from "src/models/TicketPricing";
import { ITicketPricingModel } from "src/models/TicketPricing";

export class TicketPricing {
    public calculateNormalPricing (distance:number, tClass:TClass){
        const prices:ITicketPricingModel[]=[]
        prices.
    }
}